# Stage 2 Ablation Sweep: PAPER BASELINE
# Closest match to original paper's ConvLSTM architecture
# Should get ~0.34-0.35 AP if implementation is equivalent

command:
  - ${env}
  - python
  - ${program}
  - -c
  - cfgs/convlstm/ablations/stage2_paper_baseline.yaml
  - --trainer
  - cfgs/trainer_single_gpu.yaml
  - --data
  - cfgs/data_monotemporal_full_features.yaml
  - --data.batch_size
  - "15"
  - --data.load_from_hdf5
  - "true"
  - --data.data_dir
  - /zfs/ai4good/student/antrod/data/hdf5
  - ${args}
method: grid
metric:
  goal: maximize
  name: test_AP
parameters:
  data.data_fold_id:
    values:
      - 0
      - 5
      - 10
  do_test:
    value: True
program: src/train.py
